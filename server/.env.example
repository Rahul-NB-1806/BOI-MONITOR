# MongoDB Connection String (Atlas or local)
MONGODB_URI=mongodb+srv://rahulnb1806_db_user:<db_password>@cluster0.qxt1210.mongodb.net/boi_monitor?retryWrites=true&w=majority

# JWT Secret (change in production)
JWT_SECRET=boi-monitor-jwt-secret-k8x9m2p4v6

# API Key for anonymous auth (must match Android Constants.API_KEY)
API_KEY=boi-monitor-api-key-a1b2c3d4e5

# Port (Render sets this automatically via PORT env var)
PORT=3001
